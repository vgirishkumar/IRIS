<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.temenos.interaction</groupId>
    <artifactId>interaction-parent</artifactId>
    <version>0.4.4</version>
    <relativePath>../../interaction-parent/pom.xml</relativePath>
  </parent>

  <artifactId>useragent-generic-javascript</artifactId>
  <packaging>jar</packaging>
  <name>useragent-generic-javascript</name>
  <url>http://www.temenos.com</url>
  
  <properties>
<!--  See comment below regarding issue with coffee script
  <jasmine-plugin-version>1.3.1.4-SNAPSHOT</jasmine-plugin-version>
-->
    <jasmine-plugin-version>1.3.1.3</jasmine-plugin-version>
  </properties>
  
  <build>
    <finalName>useragent-generic-javascript</finalName>
    <resources>
      <resource>
        <directory>src/main/webapp</directory>
      </resource>
    </resources>
    <plugins>
      <!-- 
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>1.1.1</version>
        <executions>
          <execution>
            <id>enforce-versions</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireJavaVersion>
                  <version>1.7</version>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.github.iron9light</groupId>
        <artifactId>coffeescript-maven-plugin</artifactId>
        <version>1.1.1</version>
        <configuration>
          <srcDir>${basedir}/src/main/javascript</srcDir>
          <outputDir>${basedir}/src/main/webapp/lib</outputDir>
          <bare>false</bare>
          <modifiedOnly>false</modifiedOnly>
          <allowedDelete>true</allowedDelete>
          <compilerUrl></compilerUrl>
         </configuration>
        <executions>
          <execution>
            <goals>
              <goal>compile</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      -->
      <plugin>
        <groupId>com.github.searls</groupId>
        <artifactId>jasmine-maven-plugin</artifactId>
        <version>${jasmine-plugin-version}</version>
        <executions>
<!--
Disabled until 1.3.1.4 of https://github.com/searls/jasmine-maven-plugin is released
If you want to see and build the tests now, build this fork locally https://github.com/aphethean/jasmine-maven-plugin
		<execution>
            <goals>
              <goal>test</goal>
            </goals>
          </execution>
-->
        </executions>
        <configuration>
          <debug>true</debug>
          <specRunnerTemplate>REQUIRE_JS</specRunnerTemplate>
          <!-- 
          Need to disable compilation within this plugin as RequireJS will 
          compile the CoffeeScript with an AMD loader plugin
          -->
          <coffeeScriptCompilationEnabled>false</coffeeScriptCompilationEnabled>
          <preloadSources>
            <include>${project.basedir}/src/main/webapp/META-INF/resources/js/require-config.js</include>
            <include>${project.basedir}/src/main/webapp/META-INF/resources/js/vendor/require-2.1.9.js</include>
            <include>${project.basedir}/src/main/webapp/META-INF/resources/js/vendor/jquery-1.8.3.js</include>
          </preloadSources>
          <jsSrcDir>${project.basedir}/src/main/webapp/META-INF/resources/js</jsSrcDir>
        </configuration>
      </plugin>
	  <!--  Skip the deployment -->
		<plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-deploy-plugin</artifactId>
			<configuration>
					<skip>true</skip>
			</configuration>
		</plugin>  
    </plugins>
    <pluginManagement>
    	<plugins>
    		<!--This plugin's configuration is used to store Eclipse m2e settings only. It has no influence on the Maven build itself.-->
    		<plugin>
    			<groupId>org.eclipse.m2e</groupId>
    			<artifactId>lifecycle-mapping</artifactId>
    			<version>1.0.0</version>
    			<configuration>
    				<lifecycleMappingMetadata>
    					<pluginExecutions>
    						<pluginExecution>
    							<pluginExecutionFilter>
    								<groupId>
    									com.github.iron9light
    								</groupId>
    								<artifactId>
    									coffeescript-maven-plugin
    								</artifactId>
    								<versionRange>
    									[1.1.1,)
    								</versionRange>
    								<goals>
    									<goal>compile</goal>
    								</goals>
    							</pluginExecutionFilter>
    							<action>
    								<ignore></ignore>
    							</action>
    						</pluginExecution>
    					</pluginExecutions>
    				</lifecycleMappingMetadata>
    			</configuration>
    		</plugin>
    	</plugins>
    </pluginManagement>
  </build>
  
</project>
