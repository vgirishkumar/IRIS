#
# Populate your responder inmemory database with some test values
#
# For example:
#foreach ( $resourceInfo in $resourcesInfo )
#if (!$resourceInfo.getEntityInfo().isFeedEntity() && $resourceInfo.getEntityInfo().isJpaEntity()) 
INSERT INTO `$resourceInfo.getEntityInfo().getClazz()`(##
#foreach ( $fieldInfo in $resourceInfo.getEntityInfo().getAllFieldInfos() ) 
`$fieldInfo.getName()`##
#set ( $indexOfLastElement = $resourceInfo.getEntityInfo().getAllFieldInfos().size() - 1 )
#if (!$fieldInfo.equals($resourceInfo.getEntityInfo().getAllFieldInfos().get($indexOfLastElement))) 
 , ##
#end
#end                                                  
) VALUES(##
#foreach ( $fieldInfo in $resourceInfo.getEntityInfo().getAllFieldInfos() )
#if ($fieldInfo.getType().equals("String")) 
'abc'##
#elseif ($fieldInfo.getType().equals("Long") || $fieldInfo.getType().equals("Integer"))
'1'##
#elseif ($fieldInfo.getType().equals("java.util.Date") && $fieldInfo.getAnnotations().contains("@Temporal(TemporalType.TIME)"))
'05:45:30'##
#elseif ($fieldInfo.getType().equals("java.util.Date"))
'2012-08-15 00:00:00'##
#else
'Type not supported'##
#end
#set ( $indexOfLastElement = $resourceInfo.getEntityInfo().getAllFieldInfos().size() - 1 )
#if (!$fieldInfo.equals($resourceInfo.getEntityInfo().getAllFieldInfos().get($indexOfLastElement))) 
 , ##
#end
#end
);	
#end
#end
